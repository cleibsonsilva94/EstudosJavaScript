const numero = 2;
const isNumeroPar = (numero % 5) === 0;
if (numero === 0) {
    console.log('O número não é válido');
}

else if (eDivisivelPor) {
    console.log('Sim');
}

else {
    console.log('Não');
}

// if (!eNumeroPar) {
// console.log('Ímpar')
//}

/* CONCEITOS IMPORTANTES 

 O símbolo % retorna o resto da divisão.
 
 O operador === é o operador de igualdade estrita em 
 JavaScript. Ele verifica se dois valores são iguais em 
 tipo e valor. Dois iguais também funcionam. A diferença 
 é que ele pode converter um texto para um valor numérico
 
 else no meio do nada não funciona. Ele é sempre no fim.
 
 ! símbolo de negação. (Linha 15)

 O "is" na frente é uma convenção para indicar que é uma 
 variável booleana.

 */